public class maxArray {
    public static void main(String[] args) {
        int[] arr = {1, 34, 21, 32, 55};
        max(arr);
    }

    static void max(int[] arr) {
        for (int i : arr) {
            
        }
    }
}
